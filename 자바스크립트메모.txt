
# includes(값,시작인덱스);

# includes나 filter등 사용시 이중배열이라면 풀어서 비교해주자
예를들어서 
v === ["값1"] // v는 ["값1"] 
또는 
v === ["1","2"] // v는 ["1","2"]
등은 걸러주지 못한다
v[0] === "값1" 이런식으로 풀어서 비교해야한다.

# findIndex((v,i,origin)=>); 
// 하나의 요소의 인덱스만 반환, 조건없음 -1 , indexOf보다 좀더 유연하게 가능


# 오브젝트에서 배열만들기 간단
Object.keys(obj).map((key)=> [a[key],key]);
// key값만 있으면 value도 불러올수있기때문에 keys로 해준다.

